class AppConstants {
  AppConstants._();

  static const String appName = 'GetX Starter Kit';
  static const int maxRetryAttempts = 3;
  static const Duration defaultTimeout = Duration(seconds: 30);
  static const Duration connectionTimeout = Duration(seconds: 15);
}
